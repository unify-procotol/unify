---
title: Form
description: Navigate through records one at a time in a detailed form view with easy record switching
---

# Form

The form layout displays data in a detailed form format, showing one record at a time with easy navigation between records. Perfect for detailed viewing and editing of individual items.

## 🎯 Basic Form Layout

<Tabs items={['Preview', 'Code']}>
<Tab value="Preview">
<UniRenderExample type="form" />
</Tab>
<Tab value="Code">
```tsx
import { UniRender } from '@unilab/unify-ui';

<UniRender
  entity={{
    name: 'User Profile',
    fields: [
      { name: 'id', type: 'number', required: true },
      { name: 'firstName', type: 'string', required: true },
      { name: 'lastName', type: 'string', required: true },
      { name: 'email', type: 'string', required: true },
      { name: 'phone', type: 'string' },
      { name: 'department', type: 'string' },
      { name: 'joinDate', type: 'date' },
      { name: 'isManager', type: 'boolean' }
    ]
  }}
  data={[
    { 
      id: 1, 
      firstName: 'Alice', 
      lastName: 'Johnson', 
      email: 'alice.johnson@company.com', 
      phone: '+1-555-0123',
      department: 'Engineering',
      joinDate: '2023-01-15',
      isManager: true
    },
    { 
      id: 2, 
      firstName: 'Bob', 
      lastName: 'Smith', 
      email: 'bob.smith@company.com', 
      phone: '+1-555-0124',
      department: 'Marketing',
      joinDate: '2023-03-20',
      isManager: false
    }
  ]}
  layout="form"
  config={{
    firstName: { label: 'First Name' },
    lastName: { label: 'Last Name' },
    email: { label: 'Email Address' },
    phone: { label: 'Phone Number' },
    department: { label: 'Department' },
    joinDate: { label: 'Join Date' },
    isManager: { label: 'Management Role' }
  }}
/>
```
</Tab>
</Tabs>

## 🎛️ Form Configuration

### Field Layout

```tsx
const formConfig = {
  // Text fields
  firstName: {
    label: 'First Name',
    required: true,
    order: 1
  },
  
  lastName: {
    label: 'Last Name',
    required: true,
    order: 2
  },
  
  // Email with validation
  email: {
    label: 'Email Address',
    type: 'email',
    required: true,
    order: 3
  },
  
  // Phone number
  phone: {
    label: 'Phone Number',
    type: 'text',
    order: 4
  },
  
  // Select dropdown
  department: {
    label: 'Department',
    type: 'select',
    options: ['Engineering', 'Marketing', 'Sales', 'HR'],
    order: 5
  },
  
  // Date picker
  joinDate: {
    label: 'Join Date',
    type: 'date',
    order: 6
  },
  
  // Checkbox
  isManager: {
    label: 'Is Manager',
    type: 'checkbox',
    order: 7
  },
  
  // Textarea for longer text
  bio: {
    label: 'Biography',
    type: 'textarea',
    order: 8
  },
  
  // Hidden fields
  id: {
    hidden: true
  }
};
```

## 🧭 Navigation Controls

The form layout includes built-in navigation controls:

- **Previous/Next buttons**: Navigate between records
- **Record counter**: Shows current position (e.g., "2 of 10")
- **Direct navigation**: Jump to specific record by number
- **Keyboard shortcuts**: Arrow keys for navigation

### Navigation Features

```tsx
// Navigation is automatically included
// No additional configuration needed
<UniRender
  layout="form"
  data={records}
  // ... other props
/>
```

## ✏️ Editing in Form Layout

Form layout is ideal for detailed editing:

```tsx
const handleEdit = (updatedRecord, index) => {
  // Update the record at the specific index
  const newData = [...data];
  newData[index] = updatedRecord;
  setData(newData);
};

<UniRender
  layout="form"
  data={data}
  generalConfig={{
    editable: true,
    showActions: true
  }}
  onEdit={handleEdit}
/>
```

## 🎨 Form Styling

### Field Grouping

Organize related fields into visual groups:

```tsx
const groupedConfig = {
  // Personal Information Group
  firstName: { label: 'First Name', group: 'personal' },
  lastName: { label: 'Last Name', group: 'personal' },
  email: { label: 'Email', group: 'personal' },
  
  // Work Information Group
  department: { label: 'Department', group: 'work' },
  joinDate: { label: 'Join Date', group: 'work' },
  isManager: { label: 'Manager', group: 'work' }
};
```

### Custom Form Width

```tsx
<UniRender
  layout="form"
  className="max-w-2xl mx-auto"
  // ... other props
/>
```

### Form Field Styling

```css
.unify-ui-render .form-layout {
  /* Form container */
  max-width: 600px;
  margin: 0 auto;
  padding: 2rem;
}

.unify-ui-render .form-field {
  /* Individual form fields */
  margin-bottom: 1.5rem;
}

.unify-ui-render .form-field label {
  /* Field labels */
  font-weight: 600;
  margin-bottom: 0.5rem;
  display: block;
}

.unify-ui-render .form-field input,
.unify-ui-render .form-field select,
.unify-ui-render .form-field textarea {
  /* Form inputs */
  width: 100%;
  padding: 0.75rem;
  border: 1px solid #d1d5db;
  border-radius: 0.375rem;
  transition: border-color 0.2s;
}

.unify-ui-render .form-field input:focus,
.unify-ui-render .form-field select:focus,
.unify-ui-render .form-field textarea:focus {
  /* Focus states */
  outline: none;
  border-color: #3b82f6;
  box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}
```

## 🔧 Advanced Features

### Conditional Fields

Show/hide fields based on other field values:

```tsx
const conditionalConfig = {
  isManager: {
    label: 'Is Manager',
    type: 'checkbox'
  },
  
  managementLevel: {
    label: 'Management Level',
    type: 'select',
    options: ['Team Lead', 'Department Head', 'VP', 'C-Level'],
    // This field would be conditionally shown
    // when isManager is true
  }
};
```

### Validation

Built-in validation for different field types:

```tsx
const validatedConfig = {
  email: {
    label: 'Email Address',
    type: 'email',
    required: true,
    // Email format validation automatic
  },
  
  phone: {
    label: 'Phone Number',
    type: 'text',
    pattern: '^\\+?[1-9]\\d{1,14}$',
    // Custom validation pattern
  },
  
  salary: {
    label: 'Salary',
    type: 'number',
    min: 0,
    max: 1000000,
    // Numeric range validation
  }
};
```

## 📱 Mobile Optimization

Form layout is naturally mobile-friendly:
- Single column layout
- Touch-friendly form controls
- Responsive field sizing
- Accessible navigation controls

## 🌟 Best Practices

1. **Logical Grouping**: Group related fields together
2. **Clear Labels**: Use descriptive field labels
3. **Required Indicators**: Mark required fields clearly
4. **Validation Feedback**: Show clear error messages
5. **Save State**: Indicate when changes are saved
6. **Navigation Hints**: Show current position in dataset

## 🔗 Related

- [Table Layout](/docs/unify-ui/components/layouts/table) - Overview of multiple records
- [Card Layout](/docs/unify-ui/components/layouts/card) - Visual record display
- [Grid Layout](/docs/unify-ui/components/layouts/grid) - Alternative detailed view 