{
  "name": "unify-api-examples",
  "version": "1.0.0",
  "description": "Examples for unify-api",
  "private": true,
  "type": "module",
  "scripts": {
    "generate-migrations": "bun run basic-usage/generate-migrations.ts",
    "basic-usage": "bun run --watch basic-usage/server.ts",
    "uniweb3": "bun run --watch uniweb3.ts",
    "orpc-server": "bun run --watch orpc-usage/server.ts",
    "orpc-client": "bun run --watch orpc-usage/client.ts"
  },
  "dependencies": {
    "@orpc/server": "^1.4.5",
    "@unify-api/cli": "workspace:*",
    "@unify-api/plugins": "workspace:*",
    "@unify-api/client": "workspace:*",
    "hono": "^4.7.11",
    "unify-api": "workspace:*",
    "zod": "^3.25.57"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
